import React from 'react';
import NotificationSystem from '../components/NotificationSystem';

const LenderNotifications = ({ user }) => {
  return <NotificationSystem user={user} />;
};

export default LenderNotifications; 